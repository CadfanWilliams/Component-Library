import styled from "styled-components";

export const Title = styled.h2`
  font-size: 1.5em;
  margin: 0;
  text-align: center;
  text-spacing: 1em;
`;

export const CardContainer = styled.div`
  border: 1px solid #662020;
  border-radius: 8px;
`;

export default { Title, CardContainer };
